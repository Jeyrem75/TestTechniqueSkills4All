{% extends 'base.html.twig' %}

{% block title %}CarCategory{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center" style="margin-top: 48px; margin-bottom: 32px;">CarCategory</h1>

        <table class="table table-bordered table-hover">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ car_category.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ car_category.name }}</td>
                </tr>
            </tbody>
        </table>
    </div>
    <div class="container d-flex align-items-center justify-content-evenly">
        <a class="btn btn-secondary" style="margin-bottom: 16px;" href="{{ path('app_car_category_edit', {'id': car_category.id}) }}">Editer</a>

        <a class="btn btn-primary" style="margin-bottom: 16px;" href="{{ path('app_car_category_index') }}">Retourner à la liste</a>

        {{ include('car_category/_delete_form.html.twig') }}
    </div>
{% endblock %}
