{% extends 'base.html.twig' %}

{% block title %}Edit Car{% endblock %}

{% block body %}
    <h1 class="text-center" style="margin-top: 48px; margin-bottom: 32px;">Editer une Car</h1>
    {{ include('car/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
    <div class="d-flex flex-column align-items-center justify-content-center">
        <a class="btn btn-primary" style="margin-bottom: 16px;" href="{{ path('app_car_index') }}">Retourner à la liste</a>
        {{ include('car/_delete_form.html.twig') }}
    </div>
{% endblock %}
