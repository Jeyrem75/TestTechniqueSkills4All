<form method="post" action="{{ path('app_car_category_delete', {'id': car_category.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer cette entrée?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car_category.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
