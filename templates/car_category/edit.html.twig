{% extends 'base.html.twig' %}

{% block title %}Edit CarCategory{% endblock %}

{% block body %}
    <h1 class="text-center" style="margin-top: 48px; margin-bottom: 32px;">Editer une CarCategory</h1>
    {{ include('car_category/_form.html.twig', {'button_label': 'Editer'}) }}
    <div class="d-flex flex-column align-items-center justify-content-center">
        <a class="btn btn-primary" style="margin-bottom: 16px;" href="{{ path('app_car_category_index') }}">Retourner à la liste</a>
        {{ include('car_category/_delete_form.html.twig') }}
    </div>
{% endblock %}
